{
  "configurations": [
    {
      "type": "bun",
      "internalConsoleOptions": "neverOpen",
      "request": "launch",
      "name": "Run Server",
      "program": "${workspaceFolder}/index.ts",
      "cwd": "${workspaceFolder}",
      "watchMode": false
    }
  ]
}
